# Rebuild State

> Single source of truth for rebuild progress. Read after README.md; update before every session ends.

## Status

- **Current phase:** Phase 1 — Core platform (☑ complete, gate passed 2026-07-21) → **Phase 2 — Auth, orgs, onboarding** (◐ in progress, segments ①②③ merged, segment ④ submitted as PR #11)
- **Target repo:** `G:\apps\gateling-tms` — created, tooling in place.
- **GitHub:** `https://github.com/Mohamed-Magdy-fayed/gateling-tms` (**public** — intentionally, see D19). Old SOURCE repo renamed to `gateling-tms-legacy` (still public) to free the name.
- **Vercel:** project `gateling-tms` (id `prj_cT7oVrlzsKpFw1TY4mWYv3XZjIGK`) — the pre-existing production project serving `tms.gateling.com` — has been **relinked** to the new GitHub repo (done by Mohamed 2026-07-19). Any push to `master` now deploys to production immediately.
- **Neon:** connection strings for preview and production provided by Mohamed, stored locally only in `.env.preview.local` / `.env.production.local` (gitignored, not committed). **Confirmed present in Vercel's Production scope** (PR #7's merge-commit deploy succeeded, which requires `DATABASE_URL` per D20's pre-build migrate step) — Preview scope not independently re-verified.
- **Phase 1 segment ① merged:** [`phase-01/i18n-drizzle-seed` → master, PR #1](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/1) — i18n engine + dictionaries, Drizzle client/config, auth+org schema slice (7 tables), 3 migrations (`0000`/`0001`/`0002`), idempotent seed harness. Merged 2026-07-19.
- **Phase 1 segment ② merged:** [`phase-01/trpc-forms` → master, PR #2](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/2) — tRPC (init/db-error/query-client/client/server, health router, `/api/trpc` route, provider wired into `_providers`) + TanStack Form system (useAppForm, FormBase, string/number/email/password/boolean/textarea/select/combobox-one fields + their shadcn/base-ui primitives). CodeRabbit reviewed: 11 actionable findings across 2 passes, all fixed in a follow-up commit (ccbf6ad) and replied to individually; second pass raised no new findings. Gates green (`check`, `build`, `audit:gate` 0 vulns). Manually verified in-browser pre-fix (temporary local-only test page, removed before commit): form fields rendered/round-tripped correctly, no console errors. Merged by Mohamed 2026-07-20.
- **Phase 1 segment ③ merged:** [`phase-01/data-table-demo-entity` → master, PR #3](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/3) — data table core (DONOR-B, 24 files verbatim) + supporting UI primitives (`calendar`, `scroll-area`, `slider`, `typography`, `alert-dialog`) + `use-mobile` hook; `demo_items` CRUD entity (org-less, migration `0003`, tRPC router on `publicProcedure`, `/demo` page with server-paginated/sorted/filtered table + CSV export + Zod-validated form dialog + delete confirm, EN/AR). Migrations verified against a fresh throwaway Postgres container, not just the already-migrated dev DB. CodeRabbit reviewed: 9 actionable findings (fixed a real CSV-parser data-integrity bug and a mutation race condition among them — see D35) + 2 of 4 nitpicks, all in follow-up commit 7b10834, replied to individually + one summary comment justifying the 3 not changed. Gates green (`check`, `build`, `test` incl. new `tests/data-table-csv.test.ts`, `audit:gate` 0 vulns). Merged 2026-07-20.
- **Phase 1 segment ④ merged:** [`phase-01/inngest-firebase-redis-proxy` → master, PR #4](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/4) — Inngest (client, one example function/event, `/api/inngest` route, adapted `docs/inngest-offload-policy.md`), Firebase (`admin.ts`/`storage.ts`, base64 upload/delete only — see D37), Redis (`integrations/redis.ts`, copied as-is), and `src/proxy.ts` skeleton (bare pass-through; locale already handled elsewhere, auth gating is Phase 2). New env vars all optional (dormant integrations, no credentials yet — see D38). `firebase-admin` pulled a transitive `uuid` vuln, cleared via a `package.json` override (D39). CodeRabbit reviewed: 4 actionable findings, all fixed in follow-up commit 472eeeb and replied to individually — including making the Inngest env check **fail closed in production** (D40). That surfaced a real pre-merge blocker (Vercel needs `INNGEST_SIGNING_KEY`/`INNGEST_EVENT_KEY` before any production build succeeds) — resolved by Mohamed connecting the **Inngest Vercel marketplace integration**, which auto-provisions both keys into Preview + Production scopes (see D41) rather than pasting them in by hand. Merged 2026-07-21; latest Vercel production deployment confirmed `READY`.
- **Phase 1 complete (gate passed 2026-07-21).** All four segments merged. Full gate re-run on merged `master`: `npm run check` (typecheck clean; Biome lint noise is the pre-existing Windows `core.autocrlf` artifact on untouched files, not a regression — see note in Phase 0 gate results), `npm run build` ✅, `npm test` ✅ (4 tests), `npm run audit:gate` → **0 vulnerabilities** ✅. Vercel production deployment `dpl_EDjZPT7icGYvuTpeLLNgfahzd2uF` confirmed `READY` (target: production), proving the D40 fail-closed Inngest check passes against Vercel's real env vars, not just local dummy values.
- **Last session:** 2026-07-23 — confirmed PR #5 and PR #6 were both merged, pulled `master`, built and submitted Phase 2 segment ② (PR #7), fixed all 15 CodeRabbit findings + 3 nitpicks in 5 follow-up commits, pushed, replied to every thread individually. Mohamed merged PR #7 same session. Pulled merged `master`, re-ran the full gate (`typecheck`, `test` 36/36, `audit:gate` 0 vulns, `build`), confirmed the Vercel production deploy for the merge commit succeeded.
- **This session (2026-07-23):** built and submitted Phase 2 segment ③ (PR #8, see below). Manually walked the invite-acceptance journey end to end with Mohamed against the real dev DB/Redis/SMTP (not just gates) — found and fixed two real bugs that only surfaced that way (`getPostAuthRedirect` dropping `returnTo`, a leaked Inngest transport error). Mohamed also started an independent background session in the same working tree to fix unstyled toasts (missing `ThemeProvider`); its two-file diff was folded into PR #8 as its own clearly-labeled commit rather than left stray. Pushed, CodeRabbit reviewed (9 actionable findings — 3 real concurrency races, a locale bug, a session-refresh bug, plus UX/pending-state nits), fixed all in a follow-up commit, replied to every thread individually + one summary comment. **Also this session:** at Mohamed's request, moved blueprint canonicity from the Desktop SOURCE copy to this repo copy (D50) — brought the repo copy fully in sync (it had been stale since Phase 0 close-out) and submitted as its own PR (#9), separate from #8. **Mohamed merged both PR #8 and PR #9** (2026-07-23, ~09:12 UTC). Pulled merged `master`, re-ran the full gate (`typecheck` clean, `test` 62/62, `build` ✅, `audit:gate` 0 vulns — `lint` clean except the same pre-existing CRLF noise, which now also touches the two new `(landing)/invite`/`(system)/organizations` page files on their first post-merge checkout, same root cause as every other flagged file, not a new issue), confirmed the Vercel production deployment for the merge commit succeeded.
- **Design handoff submitted:** [`design/theme-handoff` → master, PR #5](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/5) — Mohamed's Claude Design handoff (D34) was found sitting as **uncommitted working-tree changes directly on `master`** at the start of this session (theme tokens in `globals.css`, Fredoka/Nunito/JetBrains Mono fonts, new primitives: avatar, avatar-group, breadcrumbs, course-card, empty-state, icon-button, progress, segmented-control, stat-card, tag). Per Mohamed's instruction this session, split it into its own branch/PR rather than touching it inline with Phase 2 work. CodeRabbit reviewed: 5 actionable findings (a real accessibility bug each — see D43), all fixed in follow-up commit `5fa6eac` and replied to individually. Gates green (`check`, `build`, `test`, `audit:gate` 0 vulns). **Awaiting Mohamed's merge.**
- **Phase 2 segment ① merged:** [`phase-02/auth-core-email` → master, PR #6](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/6) — auth core (`session.ts`, `passwordHasher.ts`, `token.ts`, `oauth/{base,google}.ts`) adapted from DONOR-B with two real data-model deviations (see D42), plus nodemailer email transport + plain-HTML verification/password-reset templates (see D43 on why not `@react-email/*`) + `user/registered` → `on-user-registered` Inngest function. CodeRabbit reviewed: 8 actionable findings including one **Critical** (see D44), all fixed in follow-up commit `6cc9855` and replied to individually. Gates green (`check`, `build`, `test` incl. 14 unit tests, `audit:gate` 0 vulns). Merged 2026-07-21 (same session as PR #5).
- **Design handoff merged:** PR #5 also merged 2026-07-21, same session as PR #6.
- **Phase 2 segment ② merged:** [`phase-02/auth-flows-ui` → master, PR #7](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/7) — auth flows + UI on top of segment ①'s core (phase-02.md step 3): `signInAction`/`signUpAction`/`oAuthSignIn`/`signOutAction`/`getAuth`, Google OAuth callback (`app/api/oauth/[provider]/route.ts`, creates-or-links by email), passkey register/authenticate ceremonies (`@simplewebauthn/server`, challenges stored in `user_tokens`), password-reset OTP request/submit, email-verification resend + token consumption — plus the `sign-in`/`sign-up`/`forgot-password`/`reset-password`/`verify-email`/`passkeys` pages under `app/auth/` and a shared layout. All four auth endpoints (sign-in, sign-up, password-reset request, password-reset submit) are now rate-limited via a new `src/integrations/ratelimit.ts` (`@upstash/ratelimit`, one sliding-window limiter per endpoint — see D45). Sign-up deliberately creates a user only, no org/membership (organizations are segment ③); `getPostAuthRedirect` is org-unaware for now (verified → `/`, unverified → `/auth/verify-email`). Also ported the shadcn primitives the UI needed that hadn't landed yet (`spinner`, `loading-swap`, `button-group`, `status`, `general/{link-button,back-link}`), and added a `@` path alias to `vitest.config.ts` so unit tests can exercise modules that pull in the i18n dictionary (see D46). CodeRabbit reviewed: 15 actionable findings + 3 nitpicks, all fixed across 5 follow-up commits and replied to individually (see D47 for the security/correctness highlights) — plus an unrelated `npm audit` regression (2 new high-severity Next.js/sharp advisories that appeared between the initial push and the fix round) cleared by bumping `next` to 16.2.11 and pinning `sharp` via override. Gates green throughout (`check`/`lint` clean on every touched file — repo-wide CRLF noise on untouched files is the same pre-existing artifact as every prior gate, not a regression; `build` ✅; `test` ✅ 36 passed, 22 new; `audit:gate` 0 vulns). Merged 2026-07-23; post-merge gate re-run on `master` confirmed clean, and the Vercel production deployment for the merge commit succeeded.
- **Phase 2 segment ③ merged:** [`phase-02/organizations` → master, PR #8](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/8) — organizations feature + `orgProcedure`/`orgAdminProcedure` + proxy gating (phase-02.md steps 4–6). Real session wired into `createTRPCContext` (was hardcoded `null` since Phase 1); `orgProcedure` resolves the caller's active-org membership and injects `ctx.organizationId`/`ctx.role`, with the membership decision pulled into a pure, unit-tested function (`src/integrations/trpc/org-access.ts`) rather than inline in the middleware. Org CRUD (name-derived unique short code, retried on collision), membership management (invite/accept/updateRole/remove, guarded against zero-admin orgs via `SELECT ... FOR UPDATE`), invitation flow (token → `organization/member-invited` Inngest event → hashed token + email → `/invite/[token]` accept page), plan-limit helpers (`assertCanAddStudent/Course`, `assertStorageBudget` — defined/tested now, enforced by later domain phases per AD-6). `/organizations` settings page (profile card, members data table, invite dialog) + org-switcher dropdown. `organization_memberships.isCurrent` dropped (redundant — active org is session state, see D48) in favor of an index on `userId` (migration `0004`). Session `activeOrganizationId` is now resolved at sign-in/OAuth/passkey-auth time (`resolveDefaultActiveOrganizationId`, picks the most-recent membership) — previously only `sessionSchema` carried the field but nothing ever populated it, so even a user with a real membership got bounced to onboarding on every sign-in. Manually walking the new-invitee path end to end (with Mohamed, against the real dev DB/Redis/SMTP — not just gates) surfaced and fixed two more real bugs: `getPostAuthRedirect` silently dropped `returnTo` for unverified users (so returning to the invite link after verifying never happened), and the invite-send mutation let a raw Inngest transport error leak to the client. See D49 for the full list, including CodeRabbit's second-pass findings (three real concurrency races + a locale bug + a session-refresh bug). A `next-themes` `ThemeProvider` fix (unrelated — toasts had never had theme context since the PR #5 design handoff) was spun off as its own background task by Mohamed, completed in the same working tree, and folded into this PR as its own commit rather than a separate one given the shared branch. CodeRabbit reviewed: 9 actionable findings, all fixed in follow-up commit `486ed8e` and replied to individually + one summary comment on the un-actioned nitpick (see D49). Gates green (`check`, `build`, `test` 62 passed — 14 new, `audit:gate` 0 vulns). Merged 2026-07-23.
- **Docs consolidation merged:** [`docs/consolidate-rebuild-blueprint` → master, PR #9](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/9) — see D50. Merged 2026-07-23, same session as PR #8.
- **Phase 2 segment ④ submitted:** [`phase-02/onboarding-dashboard-seed` → master, PR #11](https://github.com/Mohamed-Magdy-fayed/gateling-tms/pull/11) — onboarding wizard + dashboard shell + seed (phase-02.md steps 7–9), the last segment of Phase 2. Ported DONOR-B's `sidebar.tsx` UI primitive verbatim. New `(system)/layout.tsx` (sidebar + org switcher + user menu with passkeys link/sign out) wraps `/demo`, `/organizations`, and a new placeholder `/dashboard` — nav deliberately lists only Dashboard + Organization settings, not Content Library/Learning Flow/Live Classes, per phase-02.md step 8's "no dead links" (those routes don't exist until Phases 4–6; no permission matrix either, per D42). `/dashboard` is now the real post-auth home: added to `proxy.ts`'s protected prefixes, and `getPostAuthRedirect`'s verified-user default moved off `/` (still the Phase-3-pending marketing placeholder) — see D51. `/get-started` (`app/(landing)/get-started/page.tsx`) branches on session state: an anonymous visitor gets a 2-step wizard (business+account info → review) whose submit (`completeOnboardingAction`) creates the user + org + admin membership together and fires `user/registered`; an already-authed-without-org user (OAuth-first entry, or a retry after org creation failed) gets a shorter business-name-only step reusing the existing `organizations.create` mutation; a signed-in user who already has an active org is redirected straight to `/dashboard` instead of seeing either form (CodeRabbit catch, see below). Extracted `createOrganizationForUser` out of that mutation so both paths share its short-code-conflict retry logic instead of duplicating it. Verify-email's success screen now offers a skippable "set up a passkey" vs. "skip, go to dashboard" prompt. Seed's baseline profile finally has a real password hash for the dev admin (closing the Phase 1 TODO) plus a teacher + 2 students, all sharing one documented dev password (`DevPass123!`, see README.md's "Seeded accounts" table) — verified locally by deleting the stale placeholder credential row, re-seeding, and confirming the new hash round-trips via the real `hashPassword` function (see D51 for the caveat this leaves for anyone with an already-seeded database, now also documented in README.md per CodeRabbit). CodeRabbit reviewed: 4 actionable findings, all fixed in follow-up commit `04f9064` and replied to individually — (1) the get-started-page session check above (a real gap: a signed-in user with an org could reach the org-only form directly by URL), (2) `get-started-wizard.tsx`'s `onSubmit` wasn't checking `completeOnboardingAction`'s returned `{isError, message}` for expected failures (rate limiting, duplicate email), silently swallowing them, (3) the legacy-seeded-credentials caveat needed to actually be in README.md, not just STATE.md, (4) a mis-named test. 9 new unit tests for the onboarding schemas. Gates green throughout (`check` — Biome clean on every touched file, the usual repo-wide CRLF noise aside; `build` ✅; `test` ✅ 71 passed; `audit:gate` ✅ 0 vulns). **Awaiting Mohamed's review/merge.**

## Next action

> **PR #11 (Phase 2 segment ④ — onboarding wizard + dashboard shell + seed, the last segment of Phase 2) is submitted, CodeRabbit's findings are fixed and replied to — awaiting Mohamed's review/merge.** When a fresh session picks this up:
> - If PR #11 is still open: check CodeRabbit/Mohamed's latest comments first — that feedback outranks starting new work.
> - If PR #11 is merged: pull `master`, re-run the full gate, then run **Phase 2's own verification gate** (phase-02.md's checklist — full get-started→verify-email→passkey-prompt→dashboard journey in EN/AR, Google OAuth round-trip, passkey register/login, session-survives-restart, `orgProcedure` cross-org FORBIDDEN test, proxy unauthed-redirect check) before declaring Phase 2 complete and moving to **Phase 3 — Landing pages**.
>
> Carried-over notes:
> - `mobile-field`, `date-field`, `date-time-field` form fields are still deferred (D28) — unchanged this session, still true.
> - **`permissions.ts` was deliberately not ported** — see D42. No segment so far has needed a richer permission matrix than simple admin-vs-not (`orgAdminProcedure`); revisit only if a future phase's role needs actually demand one.
> - **Neon/`BASE_URL`/`OAUTH_REDIRECT_URL_BASE`/Redis/SMTP confirmed present** in local `.env`; Vercel Preview scope still not independently re-verified for any of them, but Production continues to deploy `READY`.
> - **Seeded accounts are now fully usable** (closes the segment-③ carryover note) — `admin@gateling-tms.dev` / `teacher@gateling-tms.dev` / `student1@gateling-tms.dev` / `student2@gateling-tms.dev`, all password `DevPass123!`. See D51 for the one-time manual step needed on databases seeded *before* this PR.
> - **`/get-started` is real now** (closes the segment-③ carryover note) — no longer a 404.

## Phase checklist

| Phase | Status | Gate passed | Notes |
|---|---|---|---|
| 0 — Init & tooling | ☑ complete | 2026-07-19 | See gate results below |
| 1 — Core platform | ☑ complete | 2026-07-21 | All 4 segments merged (PRs #1–#4). i18n, drizzle, auth/org schema, seed, tRPC, forms, data table, demo entity, Inngest, Firebase, Redis, proxy skeleton. |
| 2 — Auth, orgs, onboarding | ◐ in progress (segments ①②③ merged, ④ submitted as PR #11) | — | PR #6: auth core + email. PR #7: auth flows/UI + OAuth + passkeys. Design handoff (PR #5) split out separately. PR #8: organizations + orgProcedure + proxy (merged). PR #11: onboarding wizard + dashboard shell + seed, CodeRabbit fixed (awaiting merge) — last segment of Phase 2. Once merged: run Phase 2's own verification gate, then Phase 3. |
| 3 — Landing pages | ☐ not started | — | |
| 4 — Content Library | ☐ not started | — | |
| 5 — Learning Flow | ☐ not started | — | |
| 6 — Live Classes (Zoom) | ☐ not started | — | |
| 7 — Excel + Google Forms import | ☐ not started | — | |
| 8 — Limits, polish, launch | ☐ not started | — | |

Statuses: `☐ not started` → `◐ in progress (step N done)` → `☑ complete (gate passed <date>)`.

## Phase 0 gate results (2026-07-19)

- ✅ `npm run check` (typecheck + Biome lint) green
- ✅ `npm run build` green (Turbopack, 2 static routes)
- ✅ `npm test` (vitest, 1 placeholder) green
- ✅ `npm run test:e2e` (Playwright, desktop + mobile projects, 1 smoke test) green
- ✅ `npm run audit:gate` → **0 vulnerabilities**
- ✅ No package outside `02-dependencies.md` (added `@base-ui/react` to the approved shadcn/ui deps row — see D18)
- ✅ Grep repo for `HBS` → zero hits in application code (only appears in `docs/rebuild/` prose describing the ban itself, and coincidentally in unrelated base64 hashes in `package-lock.json` / `.next` build artifacts)
- ✅ GitHub repo exists (`Mohamed-Magdy-fayed/gateling-tms`, public — D19) and mirrors local master (13 commits)
- ✅ CodeRabbit: authorized on the repo (confirmed — it reviewed all 4 Phase 1 PRs)
- ✅ Vercel preview deploy: exercised across all 4 Phase 1 PRs; latest production deployment confirmed `READY`
- ⏸ Neon prod DB reachable: connection strings in hand, not yet wired into Vercel env scopes (see Blockers)

## Decisions log

Decisions made during planning (2026-07-19, confirmed with Mohamed in Q&A):

| # | Decision |
|---|---|
| D1 | Single Next.js app (no monorepo). Donor code from `@workspace/*`-free single-app repos, so no flattening needed. |
| D2 | V1 scope = FREE tier only: Content Library, Learning Flow, Live Classes. All six paid modules → `05-roadmap.md` as "coming soon". |
| D3 | Platform core copied from DONOR-B (`G:\apps\gateling.com`): data table, forms, auth, i18n (has `dt()`), tRPC, Inngest, Firebase storage. |
| D4 | Tenancy: shared-schema row-level — `organizations` + `organization_memberships`, `organizationId` on every tenant table, active-org in session. Adapted from DONOR-B's branches model (renamed). |
| D5 | Auth: Redis-backed 7-day sessions, Google OAuth only, passkeys. Old app's 10-min JWT rejected. |
| D6 | Email: nodemailer ≥9.0.3 SMTP only. Resend dropped. |
| D7 | Free tier includes file uploads with an enforced 1 GB/org cap (landing copy kept as-is). |
| D8 | Live Classes copy says "powered by Zoom"; whiteboard/recording/screen-share delivered by Zoom natively. |
| D9 | Pricing page shows all 4 tiers; paid tiers badged "coming soon" with disabled/notify-me CTA. |
| D10 | Tooling: Biome + Playwright + vitest. |
| D11 | Vulnerable deps replaced, none blocked: drizzle-orm ≥0.45.2, nodemailer ≥9.0.3, next-auth/@auth/* removed, next-mdx-remote dropped, drizzle-kit ^0.31.10. "Not possible for now" ledger is empty. |
| D12 | Delivery model: segments → PRs; commit after every coherent change; CodeRabbit reviews every PR, agent fixes all findings, **Mohamed merges** (agent never merges, never commits to master). See `06-workflow.md`. |
| D13 | Databases: local Docker Postgres for dev; **Neon** for production (main branch) and per-PR preview (branch DBs). Migrations applied dev → preview → prod, generated-only. |
| D14 | Hosting: **Vercel** with per-PR preview deployments; env vars fully separated per environment scope (dev `.env` / Vercel Preview / Vercel Production). Change of host would be a new decision. |
| D15 | Code standard: human-readable, maintainable code per `06-workflow.md` §4 — applies to ported donor code as well. |
| D16 | Blueprint mirrored into TARGET `docs\rebuild\` from Phase 0; Desktop copy canonical until Phase 8 cut-over; re-mirrored in each phase's final PR. |
| D17 | GitHub repo name collision: old SOURCE repo already occupied `gateling-tms` (public, last pushed 2025-07-29). Renamed it to `gateling-tms-legacy` rather than picking a different name for the rebuild, so the new repo keeps the canonical name. Decided with Mohamed 2026-07-19. |
| D18 | Vercel project name collision: the existing `gateling-tms` Vercel project is **live in production** on `tms.gateling.com` / `courses.megz.pro` — unlike GitHub, renaming risked breaking domain bindings, so instead Mohamed relinked that same project's Git source to the new repo (2026-07-19). Consequence: every push to `master` from now on deploys straight to production; there is no separate "new" Vercel project. `@base-ui/react` added to `02-dependencies.md`'s approved shadcn/ui deps (required by the `base-mira` style DONOR-B uses); audited clean, 0 vulnerabilities. |

| D19 | The repo was flipped from private to public by Mohamed so CodeRabbit reviews stay on the free tier during the rebuild. Intentional, not an agent action. **Revert to private before/at launch** (Phase 8 close-out) — tracked as a blocker below so it isn't forgotten. |
| D20 | Vercel's Build Command was changed (by Mohamed) to run `npm run db:migrate` as a pre-build step, ahead of Phase 1's actual Drizzle setup. Added a no-op placeholder `db:migrate` script to `package.json` (2026-07-19) so production builds don't fail; Phase 1 replaces it with the real `drizzle-kit migrate` invocation. |

| D21 | `drizzle-kit` stays pinned at `^0.31.10` (not the `1.0.0-rc.4` line) — that rc requires a `drizzle-orm` 1.x line incompatible with the pinned `^0.45.2` and broke `db:generate` (`ERR_PACKAGE_PATH_NOT_EXPORTED` on `drizzle-orm/_relations`). Cleared the transitive `@esbuild-kit/core-utils` dev-server advisory instead via a `package.json` `overrides` entry pinning that dep's `esbuild` to `^0.25.0`. `npm audit` confirms 0 vulnerabilities. 2026-07-20. |
| D22 | Added `dotenv` (dep) and `tsx` (dev dep) to TARGET, not explicitly listed in `02-dependencies.md` — required to run the Drizzle seed CLI standalone outside Next's env/TS runtime, mirroring DONOR-B's own setup. Both audit-clean; add to `02-dependencies.md`'s approved list. 2026-07-20. |
| D23 | `organizations` table deviates further from DONOR-B's `branches` than a pure rename: dropped physical-branch fields (`nameEn`/`nameAr`/`addressEn`/`addressAr`/`opensAt`/`closesAt`/`mapUrl` — don't fit a SaaS org) in favor of a single `name`; kept `shortCode` as an internal slug (used as the seed natural key, see D24); added `businessName`/`phone`/`website` per `03-data-model.md`'s "business info from the get-started wizard" note, plus `plan` enum + usage counters. 2026-07-20. |
| D24 | **Seed harness is idempotent/additive-only, not clear-then-reseed** (overrides the original phase-01.md step-3 wording, per Mohamed's mid-task correction 2026-07-19/20). `npm run db:seed` looks each record up by a stable natural key and only inserts if missing; existing rows are never updated or deleted, so IDs stay stable across repeated runs. Natural keys: organization by `shortCode`, user by `email`, credentials/membership by their owning row's id. Implemented via a shared `seedIfMissing` helper in `src\drizzle\seed\base.ts`. `clear-db.ts` is ported but only reachable via a separate opt-in `npm run db:seed:clear` — never invoked by the default `db:seed` flow. This rule applies to all future seed profiles (Phase 2 onward), not just Phase 1's baseline profile. 2026-07-20. |
| D25 | Root layout (`src\app\layout.tsx`) is now a dynamic route (`ƒ`) instead of static, because it reads the `NEXT_LOCALE` cookie via `cookies()` on every request to set `lang`/`dir` — expected consequence of wiring in i18n. Phase 0's gate recorded 2 static routes; that count no longer holds from Phase 1 onward. 2026-07-20. |

| D26 | **Once a migration has been applied anywhere (including a PR's own Vercel Preview deploy), it must never be regenerated/replaced — hard rule from here forward.** PR #1's first CodeRabbit-fix commit squashed the schema fixes back into a regenerated `0000`, which orphaned the already-applied original `0000_shallow_warhawk.sql` on that branch's Preview database; the next Preview build's `db:migrate` then tried to recreate tables/types that already existed and failed (`schema`/`relation already exists`, masked by drizzle-kit's spinner CLI). Fixed by restoring the original `0000` and adding the fixes as new migrations `0001`/`0002` instead. Verified by simulating the exact Preview failure locally (DB with only `0000` applied, then `0001`+`0002` on top — succeeds) before pushing; confirmed the redeploy went READY. 2026-07-20. |
| D27 | drizzle-kit's column-rename detection needs an interactive TTY prompt (rename vs. drop+add) to disambiguate; unavailable in the agent's non-interactive shell. Workaround for the `userId`/`dateOfBirth` renames in D26: generate in two clean passes (temporarily strip the new columns to get a pure-drop migration, then restore them to get a pure-add migration) — every SQL statement still comes from `drizzle-kit generate`, none hand-written. Future single-migration renames should be run by Mohamed locally in a real terminal (answering the TTY prompt) to get a clean `RENAME COLUMN` instead of drop+add, when that matters. 2026-07-20. |

| D28 | Segment ② deliberately deferred 4 of the donor's form fields rather than copying phase-01.md's field list in full: **mobile-field** needs `react-phone-number-input`, which isn't in `02-dependencies.md` at all; **date-field**/**date-time-field** need the `Calendar` primitive built on `react-day-picker`, an approved dependency but scoped to Phase 5 there; **image-field** needs the Firebase storage helper + `uploadImage` tRPC procedure, landing together with Firebase in Phase 1 segment ④ per `04-code-donors.md`'s own note ("copy together"). All four are commented out in `src\components\forms\hooks.tsx` and `index.ts` (matching the donor's existing pattern for the still-unimplemented `search-lookup-field`), so the gap is visible at the call site rather than silently missing. Copy each one in the segment that actually unlocks its dependency — do not batch them into segment ③ by default. 2026-07-20. |
| D29 | `src\integrations\trpc\db-error.ts` uses the `postgres` package's own `PostgresError` type instead of donor's `pg-protocol` import. The donor's `import type { DatabaseError } from "pg-protocol"` only type-checks there via an incidental transitive dependency (`@types/pg` pulled in by something else in that larger repo); TARGET's `npm ls pg-protocol` resolves to nothing, and the driver we actually use (`postgres.js`, not `pg`) never throws `pg-protocol` errors in the first place — `PostgresError` (already available via the `postgres` dependency) is the correct type for the error shape this code is switching on. 2026-07-20. |
| D30 | Extended the `src\components\ui\**` biome override (already relaxing `a11y.noLabelWithoutControl` for shadcn-style primitives, added during PR #1's CodeRabbit pass) to also relax `a11y.useSemanticElements` and `a11y.useKeyWithClickEvents`. Both trip structurally on the `role="group"` div wrapper pattern used by `field.tsx`/`input-group.tsx` (donor code, unchanged) — same class of finding as the existing override, same rationale (generated-style low-level primitives, not hand-authored app forms). 2026-07-20. |

| D31 | PR #2's CodeRabbit pass caught two real bugs beyond style, worth flagging for future donor ports: (1) `databaseErrorMiddleware` (and the equivalent catch in `db-error.ts` callers) was rewrapping **every** thrown error — including intentional `TRPCError`s like `orgProcedure`'s `NOT_IMPLEMENTED` stub — into a generic 500; it now re-throws existing `TRPCError`s first. (2) `handleDatabaseError` was putting raw Postgres `cause.detail` (can contain PII, e.g. `"Key (email)=(user@example.com) already exists"`) straight into the client-facing `TRPCError.message`; messages are now generic/stable strings with `detail` logged server-side only. Both patterns exist in DONOR-B's original code too — worth a targeted check when porting other DONOR-B tRPC routers later. 2026-07-20. |
| D32 | `FormFieldProps.disabled` was donor's known-incomplete contract: `FormBase` used it only for CSS dimming, and no field component forwarded it to its actual control. Fixed across every field copied so far (number/password/textarea/boolean/combobox-one/select) and added `disabled` support to `SelectOneField`/`SelectManyField` in `general/select-field.tsx`. **Apply this when porting `date-field`/`date-time-field`/`mobile-field`/`image-field` later (D28)** — forward `disabled` to their controls too; donor's originals don't. 2026-07-20. |

| D33 | Front-loaded `react-day-picker` from Phase 5 to Phase 1 segment ③ (deviates from `02-dependencies.md`'s "install per phase — don't front-load"): `data-table-date-range-filter.tsx` — one of the data table's 24 verbatim files — needs the `Calendar` primitive, which needs `react-day-picker`. Copying the data table "verbatim" per `phase-01.md` step 6 meant either installing the dependency early or shipping a broken/stubbed file in a foundational shared module; installing early (already an approved, audit-clean dependency, just early) was the smaller deviation. Clears part of D28's blocker for `date-field`/`date-time-field` — they can land in segment ④ instead of waiting for Phase 5 if a future session wants to. `nuqs` (also listed in phase-01.md step 6) was **not** installed — the copied `use-table-url-state.ts` hand-rolls URL state via `next/navigation` and never imports it; installing an unused dependency would violate `06-workflow.md`'s "no duplicate-purpose deps" for no benefit. 2026-07-20. |
| D34 | **DONOR-B's actual visual theme (`globals.css` CSS variables/`@theme inline` block, `shadcn/tailwind.css` import, `tw-animate-css`) was never ported to TARGET** — segments ①② only wired the i18n/forms/tRPC *logic*, not the design tokens `components.json` (already copied, `style: "base-mira"`) expects. TARGET's `globals.css` is still the bare `create-next-app` scaffold (plain `--background`/`--foreground`, no `--card`/`--primary`/`--border`/etc., no dark-mode `.dark` class, no `@layer base` border/outline defaults). Consequence: every shadcn/data-table component renders structurally correct but visually bare (no borders, cards, or theme colors) — confirmed on `/demo` in segment ③. **Do not port the theme opportunistically** — Mohamed is producing a design directly (Claude Design) rather than reusing DONOR-B's `base-mira` tokens; wait for that handoff instead of copying `globals.css` from DONOR-B. 2026-07-20. |

| D35 | PR #3's CodeRabbit pass caught two real bugs beyond style in the demo-entity code (both fixed): (1) `updateDemoItem`/`deleteDemoItem` did a `findFirst` existence check, then a separate `update`/`delete` — a concurrent delete between the two could make the mutation report success on a row that no longer existed; now a single conditional write with `.returning()`, NOT_FOUND thrown when it affects zero rows. (2) `sortExpr` had no tiebreaker column, so ties in the requested sort left row order nondeterministic across paginated requests (rows could duplicate or vanish between pages); every branch now appends `asc(DemoItemsTable.id)`. Also caught in the **verbatim DONOR-B data-table copy** (fixed, since bugs get fixed regardless of origin once code is in TARGET): `csv.ts`'s `parseCsvToObjects`/`parseCsvPreview` split on `\r?\n` *before* quote-aware parsing, so a real newline inside a quoted field (which `rowsToCsv`/`escapeCell` themselves produce on export) broke the row alignment on import — rewritten as one quote-aware scan over the full text; and the `MAX_IMPORT_BYTES` guard compared `text.length` (UTF-16 code units) against a byte limit, undercounting multi-byte AR content — switched to a real `TextEncoder` byte count. Worth a targeted check when porting/using other parts of the data-table core later. 2026-07-20. |
| D36 | `calendar.tsx` had a `bg-accen` typo (range-middle days silently missing their accent background — a real DONOR-B bug, not a copy error) and two locale-formatted `toLocaleString`/`toLocaleDateString` calls with no explicit `timeZone`, a latent SSR/client hydration-mismatch risk; both fixed. `data-table-date-range-filter.tsx`'s `Calendar` usage didn't pass `dir`, so the Arabic calendar wouldn't actually render RTL despite `rdpLocale` being set correctly (`react-day-picker` doesn't infer direction from `locale` alone) — fixed with `dir={locale === "ar" ? "rtl" : "ltr"}`. All three are pre-existing DONOR-B bugs surfaced by CodeRabbit on PR #3, not introduced by the port. 2026-07-20. |

| D37 | Dropped DONOR-B's signed-URL/CORS direct-to-bucket upload path from `integrations/firebase/storage.ts` (`createSignedUploadUrl`, video MIME support, `firebase-storage-cors.json`) — Mohamed's call, mid-session 2026-07-21. Kept only the base64-through-tRPC `uploadImage`/`deleteImage` path, which doesn't need any bucket CORS configuration. Consequence for later: `image-field` (D28) will need light adaptation when it's eventually copied — DONOR-B's version likely wires to the signed-URL flow for large uploads, TARGET's storage helper no longer has that path, so it'll need to go through `uploadImage` instead (fine for course/lecture images at the existing 8MB cap; revisit only if TARGET ever needs direct large-file/video upload). 2026-07-21. |
| D38 | Segment ④'s new env vars (`REDIS_URL`/`REDIS_TOKEN`, `FIREBASE_PROJECT_ID`/`FIREBASE_CLIENT_EMAIL`/`FIREBASE_PRIVATE_KEY`/`FIREBASE_STORAGE_BUCKET`, `INNGEST_SIGNING_KEY`/`INNGEST_EVENT_KEY`) are all `.optional()` in `t3-env`, unlike DONOR-B's (mostly required) — these integrations must compile but not run yet per `phase-01.md` step 8 ("stay dormant until Phases 2/4"), and no credentials exist for any of them until their owning phase. Making them required would break `next dev`/`build` locally today. `@upstash/redis`'s and `firebase-admin`'s own types already accept `string | undefined` for these fields, so no cast/assertion was needed to keep the dormant helpers type-checking. `INNGEST_DEV=1` added to local `.env` (gitignored) so the Inngest SDK doesn't expect a signing key in dev — add the real `INNGEST_SIGNING_KEY`/`INNGEST_EVENT_KEY` to Vercel's Production scope at Phase 1 close-out (tracked in "Environment / credentials needed later" below). 2026-07-21. |
| D39 | `firebase-admin` pulled in a transitive `uuid <11.1.1` vuln (GHSA-w5hq-g745-h8pq) via `@google-cloud/storage` → `teeny-request`/`retry-request` → `gaxios`. `npm audit fix --force` wanted to downgrade `firebase-admin` to `10.3.0` (breaking, older major) — used a `package.json` `overrides` entry pinning `uuid` to `^11.1.1` instead (same pattern as D21's `@esbuild-kit/core-utils` → `esbuild` override). `npm audit` confirms 0 vulnerabilities after. 2026-07-21. |

| D40 | PR #4's CodeRabbit pass: made the Inngest env check **fail closed in production** — `src/data/env/server.ts` now `throw`s at startup if `NODE_ENV=production` and `INNGEST_SIGNING_KEY`/`INNGEST_EVENT_KEY` aren't both set (previously they were silently optional everywhere, matching D38's "must compile, don't run yet" reasoning — CodeRabbit correctly pointed out that reasoning doesn't hold for a security-relevant check: `INNGEST_DEV=1`, which `.env.example` had defaulted to, disables Inngest's request signature verification, so a deployed environment missing real keys would leave `/api/inngest` unprotected rather than just non-functional). Also fixed: `uploadImage` now validates base64 length/charset before decoding (was allocating the full buffer before the size check); `deleteImage` now only swallows confirmed 404s, logging other failures instead of hiding them; `proxy.ts`'s matcher exclusions are boundary-aware (`(?!api(?:/|$))` etc.) so `/apiary`/`/_next-data` aren't incorrectly excluded. **Consequence, flagged as a blocker below**: Vercel builds with `NODE_ENV=production` for both Preview and Production, so both scopes now need the two Inngest keys set before this PR merges, or every future deploy (production and every subsequent PR's Preview) fails to build. 2026-07-21. |

| D41 | D40's fail-closed Inngest check turned "add two env vars" into a real pre-merge blocker (Vercel builds with `NODE_ENV=production` for both Preview and Production). Resolved by connecting the **Inngest Vercel Marketplace integration** to the `gateling-tms` project (Mohamed, via the Vercel dashboard: Project → Integrations → Marketplace → Inngest → Add Integration, selecting both Preview and Production) rather than manually creating an Inngest app and pasting `INNGEST_SIGNING_KEY`/`INNGEST_EVENT_KEY` by hand. The integration auto-provisions and keeps both keys in sync going forward — same mechanism worth reaching for the next time a phase needs a marketplace-backed service (e.g. Upstash Redis in Phase 2, if it turns out to have a Vercel integration too) instead of manual env var entry. Confirmed working: latest production deployment `READY`. 2026-07-21. |

| D42 | Phase 2 segment ①'s `sessionSchema` carries `activeOrganizationId` but deliberately **no `role`** — unlike DONOR-B (one global `role` per user, checked by `permissions.ts`'s resource/action matrix), TMS roles live on `organization_memberships` (a user's role can differ per org, per AD-1/`03-data-model.md`), so a single cached session role would be wrong the moment a user belongs to two orgs with different roles. `orgProcedure` (Phase 2 step 5, segment ③) will look the role up per-request from the membership row instead. Consequence: DONOR-B's `permissions.ts` was **not ported** — its whole design keys off that same global role and has no equivalent concept (`ScreenKey`/screen registry doesn't exist in TMS either); role-gating belongs in `orgProcedure`/`orgAdminProcedure` instead. If segment ③ later needs a richer permission matrix than simple admin-vs-not checks, design it fresh against the org-membership-role model. 2026-07-21. |
| D43 | Phase 2 segment ①'s email templates (verification, password reset) use **plain HTML template strings**, not `@react-email/*` — installing it resolved to an entirely deprecated sub-package chain (`@react-email/components@1.0.12` and ~18 of its dependencies all flagged "Package no longer supported"), which `02-dependencies.md` already anticipated ("only if DONOR-B email templates need them; else plain HTML templates"). A hand-rolled `renderBaseEmail()` (`src/features/core/auth/emails/base-email.ts`) with manual HTML-escaping (user-supplied display names and URLs are interpolated into the template, so this matters for XSS) replaces it. `nodemailer` ^9.0.3 + `@types/nodemailer` installed as planned. Also: the design handoff (PR #5) surfaced 5 real CodeRabbit findings, all pre-existing in the handed-off code, not introduced this session — inaccessible `#ffffff`-on-orange/red foreground contrast in `globals.css`, an unclamped negative `max` in `avatar-group.tsx`, two components (`course-card.tsx`, `tag.tsx`) simulating keyboard clicks by casting a `KeyboardEvent` to `MouseEvent` instead of calling `element.click()`, incomplete ARIA tablist semantics in `segmented-control.tsx` (no tab panels/arrow-key nav — switched to `aria-pressed`), and `tag.tsx` nesting a real `<button>` (remove) inside a `role="button"` element when both `onClick` and `onRemove` were set (restructured to sibling buttons in that case only). 2026-07-21. |

| D44 | PR #6's CodeRabbit pass caught 8 real issues (fixed in `6cc9855`), one **Critical**: (1) `hashPassword`'s scrypt callback was missing a `return` after `reject(error)`, so an error still fell through to `hash.toString()` on a null hash — an uncaught exception outside the Promise executor's stack, not something callers could catch; added a regression test that reproduces the real async-callback timing via `setImmediate` and confirmed it actually catches the bug (reproduced the failure against the un-fixed code first). (2) `session.ts` redesigned from a single JSON blob (read-modify-write on every update) to per-field Redis hash storage (`HSET`/`HGETALL`) — `updateUserSessionData`/`setActiveOrganization`/`updateUserSessionExpiration` previously read the whole session, mutated one part, and wrote it all back, so two concurrent calls (e.g. a profile update and an org switch from different tabs) could clobber each other; each now touches only its own field. Went with per-field HSET over Redis `WATCH`/`MULTI` optimistic locking because Upstash's REST-based client doesn't support true transactions across round trips. (3) OAuth `fetchToken`/`fetchUser` had no timeout — added a shared 10s `AbortController`-based `fetchWithTimeout`. (4) OAuth state/code-verifier cookies had unconditional `secure: true`, which silently drops them on the default `http://localhost:3000/api/oauth` dev flow — now gated on `NODE_ENV === "production"` like `session.ts`. (5) `sendMail` now throws on a real delivery failure instead of swallowing it (still no-ops when SMTP simply isn't configured, the expected pre-launch state) — `on-user-registered` no longer falsely reports `{sent: true}` for an email that was never sent; a thrown error now lets Inngest's retry mechanism handle it. (6) `on-user-registered` wrapped in `step.run` and made idempotent (deletes any prior `email_verification` token before inserting), since Inngest can retry/redeliver the handler and the previous version minted+emailed a fresh token on every retry. (7) `BASE_URL`/`OAUTH_REDIRECT_URL_BASE` no longer silently default to localhost in production — fail-closed at startup exactly like D40's Inngest keys, with dev-only fallbacks moved to exported `baseUrl`/`oauthRedirectUrlBase` constants. **Consequence, same shape as D40/D41**: `next build` sets `NODE_ENV=production` even for local builds, so local `.env` (gitignored) needed these two vars added — and Vercel's Preview + Production scopes will need them too before this PR's Preview deploy (or any future deploy) can build; see Blockers. 2026-07-21. |

| D45 | Phase 2 segment ②: rate limiting (`src/integrations/ratelimit.ts`) uses one `@upstash/ratelimit` sliding-window limiter per auth endpoint (sign-in, sign-up, password-reset request, password-reset submit) rather than one shared limiter — so exhausting one flow's budget can't be worked around by hitting a different endpoint that would otherwise share it. Password-reset *submission* gets a tighter window than *request* (10/15min vs 5/60min): a 6-digit OTP only has 1,000,000 combinations, so guessing attempts need a stricter budget than requesting fresh codes does. Keyed on `ip:normalizedEmail` (IP alone would let one attacker exhaust a victim's budget from a shared key; email alone would let an attacker rotate IPs freely). 2026-07-21. |
| D46 | Phase 2 segment ②: added a `@` path alias to `vitest.config.ts` (mirrors `tsconfig.json`'s `"@/*": ["./src/*"]`) — previously absent since no unit test had needed to import a module that itself imports through the alias. `schemas.ts` does (pulls `translationKey` from the i18n dictionary for validation messages), so testing it directly required this. Low-risk, additive-only change; existing tests (which all use relative imports) are unaffected. 2026-07-21. |
| D47 | PR #7's CodeRabbit pass caught 15 actionable issues + 3 nitpicks (all fixed in 5 follow-up commits), worth flagging beyond the PR thread: (1) the OAuth callback and `signUpAction`'s session-creation failure path both put raw `error.message`/`DrizzleError.message` into `?oauthError=`/`?error=` query params, which `sign-in-form.tsx`/`sign-up-form.tsx` rendered verbatim — leaking provider/DB internals, and letting a crafted link make the app display arbitrary attacker-controlled text in a styled "error" alert. Fixed with a fixed-code allowlist (`lib/error-codes.ts`) instead of passing messages through URLs at all. (2) `validateInput`'s thrown `Error` was escaping every calling action's try/catch because it ran *before* the try block in each one (signIn/signUp/password-reset/passkey actions) — `forgot-password-form.tsx`/`reset-password-form.tsx` call these actions with no client-side try/catch, so this was a real unhandled-rejection risk, not a style nit. (3) Passkey auth had no rate limiting at all (unlike password sign-in/up), an email-enumeration leak (distinct "no account" vs "no passkeys" messages), a `userVerification: "preferred"`-vs-`requireUserVerification: true` mismatch between generation and verification, and would silently steal a passkey from its original owner if the same physical authenticator got re-registered under a different account — all fixed. (4) An unrelated `npm audit` regression surfaced mid-fix-round: 2 new high-severity advisories (Next.js CVEs + a transitive `sharp`/libvips chain) that didn't exist when PR #7 was first pushed — cleared by bumping `next` to `16.2.11` and pinning `sharp` via a `package.json` override, same pattern as D21/D39. Also worth remembering for future donor ports: running `biome check --write` on a whole directory (`src/features/core/auth`) reformatted several already-merged segment-①/PR-#6 files' CRLF line endings as an unintended side effect — reverted with `git checkout --` before committing; scope formatter `--write` runs to the specific files being touched, not a shared parent directory, when other people's already-merged code lives alongside them. 2026-07-23. |

| D48 | Dropped `organization_memberships.isCurrent` (segment ③) — Phase 1 segment ① copied it structurally from DONOR-B's branch model, but TARGET's session already carries `activeOrganizationId` (D42's own preparation) as the single source of truth for "which org is active." A DB column duplicating that would just be a second thing to keep in sync (and DONOR-B's own `isCurrent` design needs cache-tag invalidation gymnastics TARGET has no reason to inherit). Replaced with a plain index on `userId` — the column `orgProcedure` actually filters by on every authenticated request. Migration `0004`; the seed's membership insert updated to match. 2026-07-23. |
| D49 | Phase 2 segment ③'s CodeRabbit pass (PR #8) caught 9 real issues (all fixed in `486ed8e`), worth flagging beyond the PR thread: (1) **`proxy.ts` was reading the session cookie from `response.cookies`, which only reflects what's been explicitly set on the response so far (nothing, at that point)** — `updateUserSessionExpiration` silently no-op'd on every single request, so active sessions never got their expiry refreshed and would log users out on the original 7-day TTL regardless of activity. Now reads from `request.cookies`, writes to `response.cookies` via a small adapter — worth checking for the same pattern anywhere else `NextResponse.cookies` gets passed to something expecting to *read* the incoming session. (2) `updateMemberRole`/`removeMember`'s last-admin guard (count admins, then mutate) had a classic check-then-act race — two concurrent requests demoting/removing *different* admins of a 2-admin org could both read "2 admins," both pass, both proceed, leaving zero. Fixed with `SELECT ... FOR UPDATE` on the org's admin rows inside the same transaction as the check + mutation. (3) `createOrganization`'s short-code uniqueness relied solely on a pre-check `findFirst` — real fix catches the actual `23505` against `organizations_short_code_idx` and retries with a fresh code. (4) The invite-token Inngest function's delete-then-insert wasn't atomic — wrapped in a transaction guarded by `pg_advisory_xact_lock` keyed on `(organizationId, email)` rather than adding an expression unique index on the jsonb `metadata` column. (5) `send-organization-invite.ts` called `getT()`/`getLocaleCookie()` — both read a browser-sent locale cookie that doesn't exist inside an Inngest function invocation, so invite emails always silently rendered in English regardless of the inviting admin's locale. Locale is now captured from `ctx.locale` at the point the invite mutation runs (a real request scope) and threaded through the event payload — **worth checking `send-email-verification.ts` (segment ①) for the same latent bug**, since `on-user-registered.ts` runs in the identical Inngest context and wasn't touched by this PR. All three concurrency-control SQL shapes (`FOR UPDATE`, the advisory lock, the short-code constraint name) were verified directly against the real dev DB, since `server-only` blocks running `mutations.ts` standalone outside Next's build pipeline for an in-process test. Also from manually walking the invite journey end to end (not CodeRabbit, found before its review): `getPostAuthRedirect` silently dropped `returnTo` for unverified users, so an invite-triggered sign-up never made it back to `/invite/<token>` after email verification — fixed by carrying `returnTo` through as a query param and having the verify-email page redirect there on success. 2026-07-23. |

| D50 | **Blueprint canonicity moved from the Desktop SOURCE copy (`C:\Users\moham\OneDrive\Desktop\gateling-tms\docs\rebuild\`) to this repo copy (`G:\apps\gateling-tms\docs\rebuild\`), effective 2026-07-23** — ahead of the originally planned Phase 8 cut-over (README.md's original "Blueprint mirror" wording). Mohamed's instruction: the repo copy is what's actually versioned with the code on GitHub, so it should be the one every session reads/writes, not a Desktop-only folder that has to be manually re-mirrored. At the time of the switch the repo copy was stale back to Phase 0 close-out (`02-dependencies.md` missing D22's `dotenv`/`tsx` row; `STATE.md` entirely un-updated since — every Phase 1/2 decision, segment, and PR link existed only in the Desktop copy) — brought fully in sync as part of this decision, confirmed via `diff -rq` showing zero remaining differences. The Desktop copy is now retired: do not read or write it going forward; this file (and README.md) are the only copies that matter from here on. 2026-07-23. |
| D51 | Phase 2 segment ④, four related calls: (1) **`/dashboard` is now the canonical post-auth home**, distinct from `/` — `/` stays the Phase-0 create-next-app placeholder reserved for Phase 3's marketing landing page (matches SOURCE's own `/dashboard`-vs-`/` split, `src/app/(system-pages)/dashboard/page.tsx` vs `src/app/(landing-pages)/page.tsx`). `proxy.ts`'s protected prefixes and `getPostAuthRedirect`'s verified-user default both updated accordingly; the matching unit test updated. (2) **get-started collects a password**, unlike SOURCE's reference schema (`contactName`/`businessName`/`email`/`phone` only) — SOURCE's version was lead-capture only (the old app onboarded accounts manually behind the scenes), TARGET's get-started actually provisions the sign-in account, so it needs one. (3) **Seed's `SEED_DEFAULT_PASSWORD` is shared across all four baseline accounts** (admin/teacher/2 students) rather than per-account passwords — simplest thing that's still real credentials, documented in README.md, dev-only. (4) **A database seeded before this PR keeps its stale placeholder admin credential row** — the seed is additive-only by design (D24) and never updates existing rows, so upgrading the seed script's hashing logic doesn't retroactively fix rows it already inserted. Not a bug to fix in the seed script (that would violate D24); the fix is operational — delete the one stale `user_credentials` row and re-run `npm run db:seed` once. Verified this works on the local dev DB (deleted the placeholder row, re-seeded, confirmed `DevPass123!` round-trips against the new hash via the real `hashPassword` function). 2026-07-23. |

Add new decisions here as `D52`, `D53`, … with date and one-line rationale.

## Blockers

- **Neon/`BASE_URL`/`OAUTH_REDIRECT_URL_BASE` in Vercel — confirmed resolved for Production** (unchanged since last session; Preview scope still not independently re-verified).
- **Redis and SMTP credentials — resolved.** Real Upstash Redis (`REDIS_URL`/`REDIS_TOKEN`) and Zoho SMTP (`SMTP_HOST` etc.) credentials are present in local `.env` as of this session — sign-up/sign-in/passkey/OAuth and invite emails were exercised for real, not just compiled. No longer a blocker for local testing. (Vercel scope status still unconfirmed for either.)
- **Seeded dev admin still cannot sign in with a password** (carried into "Next action" above) — `baseline.ts`'s credential row is still the Phase 1 placeholder hash; not a blocker for segment work (this session worked around it via fresh sign-ups), but segment ④'s seed step needs to close it before the seeded admin is actually usable for manual testing.
- **Production is now live-wired to `master`.** Because the Vercel project was relinked (D18) instead of created fresh, every future push to `master` deploys to `tms.gateling.com` immediately — there is no staging buffer beyond PR preview deploys. Every merge request to Mohamed from Phase 1 onward should call this out explicitly.
- **Repo is public (D19), on purpose, for now.** Revert to private before/at Phase 8 launch close-out — add this to the phase-08 checklist when reached.

## Environment / credentials needed later

Collected here so no phase is surprised (fill values into TARGET `.env`, never commit):

- `DATABASE_URL` (Postgres — local docker in dev, port **5433** on `gateling_tms_db`; **Neon** connection strings for preview + production; Phase 0) ✅ local, ✅ Vercel Production (confirmed via PR #7's successful production deploy — Preview scope not independently re-verified)
- GitHub: repo under Mohamed's account (`gh auth` logged in) ✅; **CodeRabbit app installed on the repo** ✅ (active on every PR since #1)
- Vercel: project linked to the GitHub repo ✅ (relinked existing production project, D18)
- `SESSION_SECRET` / Redis: `REDIS_URL`, `REDIS_TOKEN` (var names as wired in `src/data/env/server.ts` segment ④ — donor's own naming, not `UPSTASH_REDIS_REST_*`) (Phase 2) — ✅ local (real Upstash credentials, confirmed working this session), Vercel scope status unconfirmed
- Google OAuth: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `OAUTH_REDIRECT_URL_BASE` (Phase 2, schema added in segment ①); same Google Cloud project later used for Forms API (Phase 7) — `OAUTH_REDIRECT_URL_BASE` ✅ Vercel Production (see above), `GOOGLE_CLIENT_ID`/`SECRET` still not provided (Google button will fail without them)
- SMTP: `SMTP_HOST`/`SMTP_PORT`/`SMTP_USER`/`SMTP_PASSWORD`/`SMTP_FROM_EMAIL`/`SMTP_FROM_NAME` for nodemailer (Phase 2, schema added in segment ①) — ✅ local (real Zoho SMTP credentials, confirmed this session), Vercel scope status unconfirmed
- `BASE_URL` (Phase 2, segment ①) — defaults to `http://localhost:3000` locally; ✅ Vercel Production (see above)
- Firebase: `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY`, `FIREBASE_STORAGE_BUCKET` (Phase 4) — schema present since segment ④, values not yet provided
- Inngest keys for prod: `INNGEST_EVENT_KEY`, `INNGEST_SIGNING_KEY` — ✅ provisioned automatically via the Inngest Vercel integration (D41), no manual entry needed. Local dev works keyless via `INNGEST_DEV=1` in `.env`.
- Zoom app: `ZOOM_CLIENT_ID`, `ZOOM_CLIENT_SECRET`, webhook secret (Phase 6)
